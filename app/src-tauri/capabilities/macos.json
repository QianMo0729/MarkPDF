{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "macos",
  "description": "macOS only: the toolbar doubles as the title bar, so the webview may start a window drag",
  "platforms": ["macOS"],
  "windows": ["main"],
  "permissions": ["core:window:allow-start-dragging"]
}
